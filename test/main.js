'use strict';

var assert = require('assert');
// var Fluzo = require('../lib');

describe('fluzo', function () {

   beforeEach(function (done) {
      return done();
   });

   it('...', function () {
      assert(true);
   });

});
